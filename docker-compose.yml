services:
  # ─── Produção ────────────────────────────────────────────────────────────────
  kodepost:
    build: .
    container_name: kodepost
    ports:
      - "8765:8765"
    restart: unless-stopped
    read_only: true           # filesystem read-only (segurança)
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:noexec,nosuid,size=50m
    # Para atualizar conteúdo sem rebuild da imagem, descomente o volume abaixo
    # e monte seus posts/ e templates/ no host:
    # volumes:
    #   - ./posts:/app/posts:ro
    #   - ./templates:/app/templates:ro
    #   - ./assets:/app/assets:ro
    healthcheck:
      test: ["CMD", "/kodepost"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  # ─── Desenvolvimento (opcional) ──────────────────────────────────────────────
  # Modo dev com auto-rebuild ao alterar posts/templates:
  # docker compose up --build kodepost-dev
  kodepost-dev:
    build: .
    container_name: kodepost-dev
    ports:
      - "3000:8765"
    volumes:
      - ./posts:/app/posts:ro
      - ./templates:/app/templates:ro
      - ./assets:/app/assets:ro
    command: ["serve", "-port", "8765"]
    profiles:
      - dev
